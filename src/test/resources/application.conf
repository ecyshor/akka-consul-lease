akka {
    loglevel = "DEBUG"
    actor.debug{
        receive = on
        autoreceive = on
        lifecycle = on
        unhandled = on
    }
}
consul {
    scheme = "http"
    host = "localhost"
    port = 8500
    timeout = 2 seconds
}